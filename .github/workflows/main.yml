name: First Workflow
on:
  push:
    branches:
      - main
  
jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print a welcome message
        run: echo "welcome to our first github actions worflows!"

      - name: checkout repositiry
        uses: actions/checkout@6.0.2

      - name: list repo files
        run: ls -la 
      
      

        
